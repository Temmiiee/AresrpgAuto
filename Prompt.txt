I want you to study the two project folders/codebases provided:

* `AresRPG-RL` — Python reinforcement-learning / combat research
* `AresRPGBot` — TypeScript/Bun live AresRPG bot

Your goal is to use the existing code, tests, game logic, and knowledge in BOTH projects to create a **working AresRPG combat bot that can automatically play the game and use reinforcement-learning knowledge to make its decisions**.

Do NOT blindly rewrite everything. First inspect and understand the existing implementations, tests, game mechanics, simulation, bot architecture, and differences between the two projects.

The final system should ideally have this flow:

**AresRPG game → bot observes the real fight → converts the state into the RL representation → RL/decision system evaluates possible actions → chooses the best action/plan → bot executes it in the real game → observes the result → continues until the fight ends.**

The bot should also be able to automatically:

* detect/enter fights when appropriate
* identify the fight state and available actions
* control its characters
* choose movement and abilities
* manage AP/MP, cooldowns, positioning, targeting, etc.
* adapt its decisions based on the current state
* finish fights without manual intervention
* use knowledge learned during training rather than just hard-coded combat rules

### Important existing differences

I already identified some strengths in each project.

**AresRPG-RL (Python) is currently ahead in:**

* Beam Search + Value Network
* `solve_fight.py` fight solver with explanations
* Archetype forcing in scenarios
* Reproducible/seeded evolution
* `multi_evolve.py` for independent training runs
* Value-network calibration (ECE/Brier score)

**AresRPGBot (TypeScript/Bun) is currently ahead in:**

* Parallel training with Bun Workers
* Multi-step lookahead
* `sim_decide.ts` planning an entire turn (movement + multiple AP actions)
* Real stat allocation ladder in `stat_allocation.ts`
* `spell_memory.ts` for cross-session knowledge of spells that actually work
* `caster_damage_multiplier` during training

Do not assume this comparison is completely correct. **Verify it by inspecting the actual code.**

### Main objective

Design and implement the best practical architecture for combining these systems.

I want a solution that prioritizes:

1. **Correct game logic**
2. **Correct simulation**
3. **Correct RL state/action/reward representation**
4. **Good combat decisions**
5. **Ability to learn general strategies instead of exploiting simulator bugs**
6. **Good performance during training**
7. **Ability to transfer what was learned into the real bot**
8. **A maintainable architecture**

The RL system must not learn from an inaccurate simulation. Check carefully that mechanics such as damage, healing, stats, AP/MP, movement, ranges, line of sight, cooldowns, statuses, targeting, deaths, turns, monster AI, victory conditions, and class mechanics are represented correctly wherever they are simulated.

Also check for simulator/RL problems such as:

* information leakage
* impossible actions
* incorrect state representations
* incorrect rewards
* reward exploitation
* deterministic behavior where the real game is random
* simulation mechanics differing from the real game
* training/evaluation inconsistencies
* bugs producing artificially high win rates
* the agent exploiting implementation details instead of learning useful combat
* actions being evaluated differently during training and live play

### Architecture

Before implementing anything, determine whether the best architecture is:

* Python RL engine + TypeScript live bot
* TypeScript/Bun RL engine + live bot
* a shared/core simulation with an RL layer
* or another architecture

Do not choose based on language preference. Choose based on the actual code and what gives the most reliable result.

If Python is significantly better for the RL algorithms but TypeScript is significantly better for interacting with the real game, **keep both if necessary and create a clean interface between them** rather than unnecessarily porting everything.

For example, investigate whether the RL system could expose a decision service/API or another efficient interface that the TypeScript bot can call during combat.

### Use the existing code

Reuse existing working implementations wherever possible.

In particular, investigate whether the final system can combine:

* Python Beam Search
* Value Network
* `solve_fight.py`
* seeded/reproducible evolution
* `multi_evolve.py`
* value calibration
* Bun parallel workers
* multi-step lookahead
* `sim_decide.ts`
* real stat allocation
* `spell_memory.ts`
* `caster_damage_multiplier`

Do not duplicate functionality if an existing implementation can be adapted.

### Very important: tests

Use the existing tests extensively.

Before trusting the RL system, create or improve tests proving that:

* simulator results match known game behavior
* actions have the expected consequences
* state serialization/deserialization is correct
* legal actions are actually legal
* illegal actions cannot be selected
* rewards are correct
* the same seed produces reproducible results where expected
* the RL model receives the correct information
* the live bot interprets the RL decision correctly
* the live bot executes the intended action correctly
* the same combat situation produces consistent decisions when deterministic
* training and live inference use compatible state/action formats

Where possible, create small deterministic combat scenarios specifically designed to catch logic errors.

### Implementation priority

Do not spend most of the time building an extremely sophisticated RL algorithm before we have a reliable end-to-end bot.

Prioritize getting this working:

**observe → represent state → decide → execute → observe result → repeat**

Then improve the decision system.

The final bot should have a sensible fallback if the RL system cannot make a valid decision. It must never execute an invalid action just because the model predicted it.

### Expected deliverables

At the end, provide:

1. A clear explanation of the architecture you chose.
2. The important changes made to both projects.
3. The complete code required for the working prototype.
4. Instructions to train the RL system.
5. Instructions to load/use the trained knowledge.
6. Instructions to launch the automatic AresRPG bot.
7. Explanation of how the RL system communicates with the live TypeScript bot.
8. Explanation of how state/action representations correspond between training and live play.
9. Tests proving that the important game logic works.
10. Known limitations and remaining work.
11. A list of anything you deliberately did NOT implement and why.

Most importantly:

**Do not just give me suggestions or pseudocode. Inspect the actual folders and implement the strongest practical version you can from the existing code.**

If something in the existing implementation is fundamentally wrong, fix it rather than building on top of it.

The final objective is not merely to have an RL training project or a scripted bot. The objective is a **real autonomous AresRPG bot whose combat decisions are driven by learned knowledge, with a simulator and RL pipeline accurate enough that the learned behavior transfers correctly to the real game.**
